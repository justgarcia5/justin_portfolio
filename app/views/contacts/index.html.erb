
<% @contacts.each do |contact| %>
  <div class="container" style="margin-top: 100px;">
    <%= contact.name %>
    <%= contact.email %>
    <%= contact.message %>
    <%= contact.created_at %>
  </div>
<% end %>
